// var data = [100,200,300,50,30]
// let rectWidth = 100
// let height = 300

// var svg = d3.select('svg')

// svg.selectAll('rect')
// .data(data).enter().append('rect')
// .attr('x', (d, i) => i*rectWidth)
// .attr('y', (d, i) => height - d)
// .attr('width', rectWidth)
// .attr('height', d => d)
// .attr('fill', 'blue')
// .attr('stroke','#fff')
